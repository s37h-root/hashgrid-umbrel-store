version: "3.7"

services:
  app:
    image: ghcr.io/s37h-root/hashgrid-bridge:v1.0.0
    network_mode: host
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR:-./data}:/data
    environment:
      - DATA_DIR=/data
      - PORT=3000
